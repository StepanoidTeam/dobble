{
  "rules": {
    "rooms": {
      ".read": "auth != null",
      ".indexOn": ["status"],
      "$roomCode": {
        ".write": "auth != null && (!data.exists() || root.child('rooms/' + $roomCode + '/hostUid').val() === auth.uid)",

        "players": {
          "$uid": {
            ".write": "auth != null && ($uid === auth.uid || root.child('rooms/' + $roomCode + '/hostUid').val() === auth.uid)",
            ".validate": "newData.hasChildren(['displayName', 'score', 'cardsWon', 'connected', 'joinedAt'])"
          }
        },

        "rounds": {
          "$roundIndex": {
            ".write": "auth != null && !data.exists()",
            ".validate": "newData.hasChildren(['winnerId', 'symbol', 'timestamp']) && newData.child('winnerId').val() === auth.uid"
          }
        },

        "game": {
          ".write": "auth != null && root.child('rooms/' + $roomCode + '/hostUid').val() === auth.uid",

          "playerCards": {
            "$uid": {
              ".write": "auth != null && $uid === auth.uid"
            }
          },

          "currentRound": {
            ".write": "auth != null"
          },

          "centralCard": {
            ".write": "auth != null"
          },

          "nextDrawIndex": {
            ".write": "auth != null"
          }
        },

        "lastActive": {
          ".write": "auth != null"
        },

        "wrongTaps": {
          "$uid": {
            ".write": "auth != null && $uid === auth.uid"
          }
        },

        "abilities": {
          "$targetUid": {
            ".write": "auth != null"
          }
        },

        "status": {
          ".write": "auth != null && (root.child('rooms/' + $roomCode + '/hostUid').val() === auth.uid || newData.val() === 'finished')"
        }
      }
    }
  }
}